.ui.segment

%h1 Details Page
%h2 Hi, #{@people.first.email}

= link_to 'Edit', edit_profile_path

= image_tag @people.first.avatar.url(:medium)


%iframe{:width => 600, :height =>900, :src => @people.first.resume.url, :frameborder =>"0"}
/ = form_for profile_path, html: { multipart: true} do |form|
/   = form.file_field :avatar
%table
  %tbody
    %td= @people.first.username
    %td= @people.first.username
    %td= @people.first.company
    %td= @people.first.position
    %td= @people.first.start_date
    %td= @people.first.description